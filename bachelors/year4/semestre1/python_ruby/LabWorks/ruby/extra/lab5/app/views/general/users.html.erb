<ul>
  <% @users.each do |user| %>
      <li>
        <%= link_to user.username, profile_path(user)%>
        <% if current_user %>
          (<%= link_to 'Befriend', befriend_path(user), method: :post %>)
        <% end %>
      </li>
  <% end %>
</ul>